import React, { Component } from 'react'
import Trick from '../utils/Trick'

export default class Header extends Component {
    render() {
        return (
            <div>
                <Trick/>
            </div>
        )
    }
}
